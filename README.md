# KrakenLauncher

To begin development simple clone the project, then run `npm install`

This will install all the required modules, to start the project simply type `npm start` and in a separate terminal `npm run electeon`
